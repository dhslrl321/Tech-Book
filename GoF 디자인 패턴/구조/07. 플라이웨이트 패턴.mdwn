# xx 패턴

> 공유를 통해서 많은 수의 소립 객체들을 효과적으로 지원한다.

- 객체의 내부에서 참조하는 객체를 직접 만들지 않고, 객체를 공유한다.
- 결국 객체 수를 줄이고 메모리 사용 공간을 줄여 성능을 향상시키는 데 사용된다.
- 플라이급 : 48 <= 플라이급 <= 51 선수들
  - **메모리**를 가볍게 유지

# 등장 배경

- 어떤 인스턴스가 아주 많이 필요하지만 모두 같은 방식으로 제어할 때

# 참여 객체

# 예시

# 장점과 단점

### 장점

- 객체 인스턴스의 개수를 줄여서 메모리를 절약한다.
- 여러 가상 객체의 상태를 한 곳에 집중시킬 수 있다.

### 단점

- 플레이웨이트 패턴을 구현하면 특정 인스턴스만 다른 방식으로 동작하도록 하는 것이 불가능하다.

# 만약 패턴을 사용하지 않는다면?
